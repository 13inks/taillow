# Tailnet node state (holds the node key). Never commit it.
.tsnet-state/
# Secrets live in the environment only.
.env
/bin/
# The audit log names who asked for what. It stays on the machine that wrote it.
*.jsonl
